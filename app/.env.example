# Copy this to .env and fill in your values
PORT=3000
APP_VERSION=1.0.0

# Docker registry credentials (used by Jenkins)
DOCKER_REGISTRY=docker.io
DOCKER_IMAGE_NAME=your-dockerhub-username/sample-app
